package com.elyria.app.core.constants

import androidx.compose.ui.graphics.Color

/** Mood accent colors for selectors and charts (not Material theme tokens). */
object MoodColors {
    val lavender = Color(0xFFC4B5FD)
    val beige = Color(0xFFE8DCC8)
    val growth = Color(0xFF7ED321)
}
